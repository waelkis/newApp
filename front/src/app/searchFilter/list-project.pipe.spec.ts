import { ListProjectPipe } from './list-project.pipe';

describe('ListProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ListProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
