import { ListWorksPipePipe } from './list-works-pipe.pipe';

describe('ListWorksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ListWorksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
